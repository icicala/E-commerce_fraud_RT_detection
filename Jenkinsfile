pipeline {
    agent {label "linux"}
    stages {
        stage('Hello') {
            steps {
                echo "hello from Jenkinsfile_RT"
            }
        }
        // Add more stages if needed
    }
}
