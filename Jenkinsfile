pipeline {
    agent {label "linux"}
    stages {
        stage('Hello') {
            steps {
                // Your build steps here
            }
        }
        // Add more stages if needed
    }
}
